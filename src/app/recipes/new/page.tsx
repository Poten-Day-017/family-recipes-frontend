import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "...",
  description: "...",
};

const Recipe = () => {
  return <div></div>;
};

export default Recipe;
